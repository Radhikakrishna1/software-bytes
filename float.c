#include <stdio.h>
int main()
{
    int a;
    float f,sum;
    a = 5;
    f = 8.5;
    sum =a + f;
    printf("this is the value of %d \n", a);
    printf("this is the  value of %f\n", f);
    printf("the sum of a and f is %f \n",a + f);
    return 0;
}
